export interface ITheme {
  mainColor: string;
  secondaryColor: string;
  thirdColor: string;
  textColor: string;
  keysTextColor: string;
  keysBgColor: string;
  keysShadow: string;
  resetBgColor: string;
  resetTextColor: string;
  computeBgColor: string;
  computeTextColor: string;
}
